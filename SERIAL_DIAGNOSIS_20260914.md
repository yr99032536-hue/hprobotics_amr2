# 모터 정지/저속 진단 (2026-09-14)

## 최종 후속 교정: PID124 최소값 복구

키보드 정지 후 실제 모터 193/200을 각각 3회 읽어 RPM=0, ref_rpm=0,
controller_status=0을 확인했다. PID124=13825를 **2바이트 값 0**으로 교정했다.
직후 PID153/154는 여전히 13825였지만 후속 읽기에서는 둘 다 100으로 갱신됐다.
PID109/112도 100으로 확인했다. 즉 가감속 최소값 교정 후 정상 범위로 돌아왔다.
PID153/154/109/112 쓰기는 실행되지 않았다(예상값 검사에서 중단).
PID124만 0으로 두 번 썼으며, 전류/물리 비상정지/브레이크 설정은 교정 대상이 아니었다.
PID143=120, PID122/221=3000은 유지됐다. 전압의 정상 여부는 별도 판단 대상이다.

기존 잘못된 PID124 쓰기는 앞서 제거되어 재발 방지 코드가 적용되어 있다.
목표/램프/실측 RPM과 상태 비트를 함께 남기도록 피드백 로그를 보강했다.
회귀 테스트 68개 통과. 실제 주행 성능은 아직 검증하지 않았다.

SLAM을 재시작하지 않고 모터만 현재 정지 odom
(-24.845410634226116, -37.299251681508224, yaw=-2.1382440017052216)으로 재기동했다.
지도 백업: maps/backups/before_min_delay_repair_20260914_1 (.posegraph/.data).
키보드는 시작 속도 0.18 m/s, 회전 0.2 rad/s로 복구했다. 자동 이동 명령은 보내지 않았다.
아래 내용은 이 교정 이전의 진단 이력이다.

## 확인된 사실

- 지속적인 키보드 전진 명령에도 모터 피드백은 수 초~십여 초 동안 천천히 증가했다. 장애물 차단만으로 설명되지 않는다.
- 정지 상태에서 PID 4 요청 형식으로 실제 컨트롤러 레지스터를 읽었다. 이동 명령은 보내지 않았다.
- PID 124, 153, 154: 각각 `01 36` = 13825. PID 109, 112도 동일. 문서상 가감속 설정 범위 0~1023과 맞지 않는다.
- PID 143: `78 00` = 120 (0.1 V 단위라면 입력 12.0 V). 정격 전압/부하 중 전압은 확인하지 못했으므로 배터리 정상 또는 부족을 확정하지 않는다.
- PID 193/200 정지 시 상태 값은 0, 온도는 26도였다. 정상 주행을 보장하는 증거는 아니다.
- PID 108/111/121 응답 없음 등 펌웨어/문서 차이가 있어 하드웨어 설정값을 추측해서 덮어쓰지 않았다.

## 수정한 코드

`md200t_driver.py`가 PID 124를 브로드캐스트 스위치로 오인하여 시작/종료 때 1바이트를 쓰던 코드를 제거했다. 제조사 문서에서 PID 124는 2바이트 MIN_SSSD이다. 해당 오기록이 이상 가감속 값의 원인일 가능성이 높지만 재현 및 복구 주행 시험은 아직 하지 않았다.

레지스터 읽기 요청을 PID 4 / 길이 1 / 요청 PID 형식으로 수정했다. 다른 PID 응답은 건너뛰고 읽기 시간 제한과 응답 전체 길이 검사를 추가했다. 회귀 테스트를 추가했다.

안전정지, 장애물 거리, 속도 상한은 이번 수정에서 변경하지 않았다. 하드웨어 레지스터는 읽기만 했으며 비상정지/전류보호는 해제하지 않았다.

## 남은 작업

1. 배터리/전원 정격과 컨트롤러 모델·펌웨어 확인.
2. 제조사 설정 도구 또는 해당 펌웨어 사양으로 MIN_SSSD 및 가감속 정상값 확인 후 백업·복구·재읽기.
3. 안전한 저속 주행으로 명령 RPM 대비 실제 피드백과 부하 전압 확인.
4. 모터 프로세스가 종료되어 마지막 odom을 확보하지 못했다. 오래된 초기 좌표로 기존 SLAM에 재연결하지 말 것. 지도 백업과 위치 연속성 복구가 먼저다.

## 근거

## 후속 지도 연결 복구

사용자가 종료 후 로봇을 옮기지 않았다고 확인했다. 백업 `.data`를 설치된 Karto SDK로 읽어 마지막 스캔 ID 659의 odometric pose를 추출했다:
`x=-24.064689023955779, y=-33.049534527037842, yaw=-0.076934041037836992`.
이는 마지막 저장 스캔 위치이며 정확한 종료 시점 위치와 동일하다고 보장하지 않는다.
해당 초기 추정으로 모터 노드를 재기동했다. SLAM은 재시작하지 않았다.
주행 토픽은 `/control/cmd_vel_recovery_hold`로 격리했고 키보드 노드는 시작하지 않았다.
하드웨어 가감속 비정상 값이 남아 있으므로 이 상태는 지도 좌표 연결 복구이지 주행 복구 완료가 아니다.

### 통신 문서

후속 사용자 요청으로 수동 키보드 조작 연결을 복구했다. 모터/SLAM 재시작 없이
`helper_keyboard_teleop`를 `/control/cmd_vel_recovery_hold`에 연결했다.
따라서 토픽 이름과 기존 모터 창 제목에 hold가 남아 있어도 이제 주행 입력이 연결된 상태다.
키보드 시작 속도 0.05 m/s, 회전 0.2 rad/s, timeout 0.25 s.
기존 방향별 안전 게이트와 상한은 유지했다. 이동 키 입력이나 실제 이동 시험은 하지 않았다.
컨트롤러 가감속 비정상 읽기값은 미해결이며 연결 복구를 하드웨어 정상 판정으로 해석하지 않는다.

- 제조사 RS485 V4.7 원문 사본: https://www.scribd.com/document/642467825/RS485-communication-protocol-V47 (PID 124, 153, 154)
- 제조사에서 소개하는 ROS 드라이버 요청 구현: https://github.com/c-jho/MD_controller/blob/main/md_controller/src/com.cpp (PID_REQ_PID_DATA)
