# 방향별 장애물 차단 — 저속 시험 기능

## 최신 상한: 0.375m/s / 1.5rad/s

사용자가 현재 상한의 3배를 명시적으로 요청해 이동 0.125→0.375m/s,
회전 0.5→1.5rad/s로 변경했다. 여유 0.06m와 기존 후방 필터는 유지한다.
수집 반경은 최대 2초 이동거리로 자동 확대된다. 회귀 시험 65개 통과,
1m 전방 장애물을 새 최대속도에서 검사하는 회귀 시험도 포함한다.
실제 최고속도 제동 성능과 후방 안전은 검증되지 않았다.
키보드 시작값은 0.05m/s, 0.2rad/s로 유지하고 사용자가 키로 조절한다.
지도·SLAM은 유지하고 정지 좌표를 이어받아 모터만 재기동한다.
백업 기준 경로: `maps/backups/before_speed_0375_20260914_1`.
아래 수치는 이전 변경 기록이다.

## 최신 변경: 여유 거리 6cm / 진행 중 매핑 유지

사용자 요청에 따라 충돌 검사 여유 margin을 0.12m에서 0.06m로 줄였다.
이는 실제 제동거리가 아니라 예측 궤적의 차체 외곽에 더하는 여유다.
2초 궤적 예측과 속도 상한 0.125m/s, 0.5rad/s는 그대로 유지한다.
키보드 시작 속도는 0.05m/s, 0.2rad/s로 복원한다.
경계값/실제 장애물/센서 실패/입력 만료를 포함한 회귀 시험 64개가 통과했다.
최고속도에서의 실제 제동 성능을 검증한 것은 아니며 후방 제외 구간도 유지된다.

매핑은 SLAM을 재시작하지 않고, 정지 시점의 odom x/y/yaw를 이어받는 모터
교체로 계속한다. 백업 파일 기준 경로:
`maps/backups/before_margin_6cm_20260914_1` (.pgm/.yaml/.posegraph/.data).
아래 이전 변경 기록의 12cm는 이번 변경 전 값이다.

## 최신 속도 상한 변경

사용자 요청으로 이동 상한 0.05→0.125 m/s, 회전 상한 0.2→0.5 rad/s로
각각 2.5배 조정했다. 모터 입력 제한과 충돌 검사 속도 제한을 공통 상수로
통일했다. 충돌점 수집 반경도 고정 0.8m 대신 차체·여유·최대 이동거리로 계산한다.
2초 예측, 약 1cm 궤적 표본, 0.12m 여유, 입력/센서 watchdog은 유지한다.
기존 후방 제외 구간은 그대로이며 상한 증가가 제동 안전성 검증을 뜻하지 않는다.
키보드 시작 속도는 0.05m/s, 0.2rad/s로 유지하며 w/e/q로 새 상한까지 올린다.
실제 최고속도 주행 시험은 별도로 필요하다.

## 최신 수정: 부분 측정 누락과 키 반복

회귀 시험 55개 통과(150ms 첫 반복과 250ms 입력 만료 정지 포함).
수정 모터는 `AMR MOTOR - scan health fixed` 창에서 별도로 기동한다.
모터만 교체하고 SLAM/지도는 유지하며, 정지 좌표를 이어받는다.

유효 거리 50% 미만 / 연속 누락 10도 초과에 따른 전체 정지를 제거했다.
누락 비율과 각도는 진단값으로만 남긴다. 스캔 시각 0.3초, TF, 전체 거리 무효,
불완전한 360도 메타데이터 검사와 실제 장애물 차단은 유지한다.
부분 누락을 허용하는 것은 해당 공간이 안전하다는 보증이 아니다.

키보드 정지 timeout은 0.25초로 유지했다. GNOME 반복 시작 지연만
500ms에서 150ms로 변경했고 X 서버에서도 150ms/약 33Hz를 확인했다.
이 데스크톱 설정은 다른 프로그램에도 적용된다. 원래 설정 복원 명령:
`gsettings set org.gnome.desktop.peripherals.keyboard delay 500`
복원하면 현 키보드 timeout과의 초기 반복 간격 불일치가 다시 생길 수 있다.

## 사용자 요청: 기존 각도·거리 필터 호환 모드

적용 완료: 별도 `AMR MOTOR - legacy scan filter` 창으로 모터만 재기동했고
실행 파라미터 true를 확인했다. 회귀 시험 54개 통과, 실시간 재생 60프레임에서
전후진/좌우 회전 모두 clear였다. 이동 명령은 보내지 않았으므로 실차 회전
성공을 검증한 것은 아니다. SLAM/라이다/지도는 유지했으며 TF 연결도 확인했다.

`directional_legacy_scan_filter:=true` 옵션을 추가했다. 기존 SLAM 필터처럼
라이다 좌표 -40~+40도 내부와 0.15~8m 밖의 거리점을 충돌 검사에서 제외한다.
이것은 자기 반사를 확정해 제거하는 기능이 아니다. 현재 yaw=pi 장착에서는
후방 약 80도와 근거리 사각지대가 생기며 실제 장애물도 제외될 수 있다.
후진·회전 허용은 해당 방향의 안전 확인을 뜻하지 않는다.
현장 관찰자가 주변 여유를 확인하는 저속 수동 시험에만 사용한다.

원본 스캔의 건전성/TF/시각 검사는 필터 적용 전에 계속 수행하며, 속도 제한과
정지 후 키 해제 잠금은 유지한다. 기존 SLAM 스캔 토픽은 변경하지 않는다.
전체 기동 때 선택하려면 기존 명령에 다음 두 옵션을 함께 사용한다:

```bash
ros2 launch helper_navigation manual_slam_bringup.launch.py directional_safety_enabled:=true directional_legacy_scan_filter:=true
```

이미 기동 중일 때 위 명령을 추가 실행하면 노드가 중복된다. 기본값은 false로,
옵션을 생략하면 원본 거리점 검사 방식이다. 아래 v2 설명은 이 옵션을 끈 경우다.

## 2026-09-14 수정 내용

이전의 광선 보간/가상 차체 가림 검사를 제거했다. 사용자가 회전부가 노출돼
있음을 확인한 360도 라이다를 전제로 한다. 2D 차체 투영만 보고 후진/회전을
무조건 사각지대로 판정하던 것은 잘못된 가정이었다.

Nav2 Collision Monitor의 유효 거리점과 충돌 영역을 비교하는 원리를 참고했다.
**Nav2 노드로 교체한 것이 아니라 기존 모터 내부 검사기를 수정한 것이다.**
설치된 패키지는 ros-humble-nav2-collision-monitor 1.1.20이다.
최신 배포판의 velocity polygon/센서 실패 처리 기능을 Humble에 그대로 있다고
가정하지 않는다.

참고:
- [Nav2 Collision Monitor](https://docs.nav2.org/jazzy/configuration_and_development/configuration_guide/core_servers/collision_monitor/configuring_collision_monitor_node/)
- [Humble의 실제 LaserScan 처리 코드](https://api.nav2.org/nav2-humble/html/scan_8cpp_source.html)
- [공식 사용 예제와 Humble 파라미터 차이](https://docs.nav2.org/jazzy/tutorials/general_tutorials/using_collision_monitor/using_collision_monitor/)

## 동작 구분

1. **방향별 장애물 정지**: 유효한 실제 거리점을 base_link로 변환하고
   전진/후진/회전/곡선 명령의 2초 예상 궤적과 비교한다.
   여유 영역 안에서 장애물에 더 접근하면 명령 전체를 0으로 한다.
   반대 방향은 그 경로가 허용되는 경우에만 가능하다. 곡선 명령을 임의로
   직진으로 바꾸지는 않는다.
2. **센서/TF 이상 정지**: 스캔 없음, 미래 시각, 0.3초 초과,
   TF 미확보, 기울어진 장착, 불완전한 360도 범위이면 차단한다.
   거리값 전체가 무효인 경우도 차단한다. 부분 누락의 비율/각도만으로는
   차단하지 않는다(위 최신 수정 참고).
3. **재출발 잠금**: 차단 뒤에는 키를 놓아 입력 만료를 기다리거나 k로 0을
   보내고 새 방향을 입력한다. 누른 채 장애물이 사라져도 자동 출발하지 않는다.
4. **물리 비상정지**: 별도 하드웨어 기능이다. 이번 코드는 그 회로를 변경하거나
   정상 작동을 검증한 것이 아니며, 하드웨어 비상정지 상태 입력도 추가하지 않았다.

일부 +Inf/NaN/범위 밖 값은 장애물 점으로 만들지 않는다. 그렇다고 빈 공간이
증명된 것은 아니다. 넓은 누락/센서 단절은 별도 검사가 막는다.
작은 실제 유효 거리점은 하나라도 유지한다. Nav2의 점 개수 임계값을 올려
가는 물체를 놓치는 설정은 이번에 적용하지 않았다. 자기 반사처럼 보이는 점도
임시 직사각형 안이라는 이유만으로 지우지 않는다. 지속되는 근거리 반사는
실물 확인 및 정확한 자기 반사 마스크 보정이 필요하다.

## 치수·제한

- 검사 영역: base_link 기준 x=[-0.40, 0.215], y=[-0.23, 0.23] m.
  앞쪽 값은 URDF 카메라 돌출 끝 0.1847+0.03 m를 포함하도록 수정했다.
  폭은 기존 보수적 값을 유지했다. 실제 외형 실측은 아직 필요하다.
- 여유 거리 0.12 m, 궤적 표본 간 최대 모서리 이동 약 1 cm.
- 최고 0.05 m/s, 0.2 rad/s. 키보드 증속으로 넘지 못한다.
- 구동 윤거는 URDF와 모터 모두 0.190 m. 외형 폭과 윤거는 별개다.
- 단일 라이다 평면 위/아래 장애물, 유리, 검은 물체, 움직이는 사람,
  실제 차체 가림은 이 알고리즘으로 검출이 보장되지 않는다.
  노출된 센서라도 실제 사각지대가 생기면 장착/센서 구성을 보완해야 한다.
- 안전 인증 기능이나 정지거리 보증이 아니다. 물리 비상정지를 대체하지 않는다.
- 방향별 모드에서 기존 scalar fused/range/depth 판정은 사용하지 않는다.
  깊이 카메라의 3D 장애물 판정을 통합한 기능은 아니다.

## 검증 및 적용

이번 수정본은 빌드 후 모터 노드에 적용했다. 창 제목은
`AMR MOTOR - finite-point safety v2`이며 기동 로그에 같은 버전 문구가 나온다.
라이다와 SLAM은 재시작하지 않았다. 재기동 후 실시간 61프레임에서
전진/후진은 61회 모두 허용, 좌회전은 61회 차단, 우회전은 60회 차단됐다.
회전 차단 사유는 실제 거리점에 대한 `obstacle_in_path`였다.
유효 거리 비율 최소 86.8%, 연속 누락 최대 4.51도였다.

0.03 m/s, 최대 0.3초의 실차 명령 시험에서 6개 명령을 전달했고
`directional safety: clear`로 종료했다. 최종 보고 속도는 0이었다.
기록된 PID210 피드백은 0 RPM이며 처음 한 번 읽기 실패도 있었다.
따라서 **실제 바퀴 회전/60cm 이동 성공은 아직 확인되지 않았다.**
약 2.3mm odom 증가는 명령 추정일 수 있어 이동 증거로 사용하지 않는다.

회귀 시험 51개: 전후진/회전, 접근 장애물 차단, 반대 방향 탈출,
단일 근거리 점 유지, 작은 측정 누락 허용, 넓은 누락/센서·TF 실패 정지,
키 해제 잠금, 속도 제한, 직렬 응답 timeout을 검사했다.
저장한 실차 스캔 3프레임도 재생한다. 이 시험은 실제 바퀴 이동 성공을 뜻하지 않는다.

```bash
cd /home/hprobot/amr_ws
source /opt/ros/humble/setup.bash
source install/setup.bash
PYTHONPATH=src/helper_control:$PYTHONPATH python3 -m pytest -q \
  src/helper_control/test/test_directional_safety.py \
  src/helper_control/test/test_directional_motor_gate.py \
  src/helper_control/test/test_feedback_deadline.py
```

기존 프로세스에는 Python 수정이 자동 반영되지 않는다.
모터 노드 재시작 시 키보드를 종료하고 정지 명령/정지 상태를 확인한 다음
현재 /control/odom의 x/y/yaw를 initial_odom_x/y/yaw로 전달한다.
SLAM/라이다는 유지한다. **과거 스냅샷 숫자를 다음 재시작에 재사용하지 않는다.**
전체 기동 기본 directional_safety_enabled는 false이며, 새 모드는
directional_safety_enabled:=true일 때만 사용된다.
별도 모터 창과 기존 매핑 창이 함께 있으면 종료 때 둘 다 구분해야 한다.

로그:
- obstacle_in_path: 요청 궤적에서 실제 거리점에 접근
- scan_coverage_insufficient: 광범위 누락/불완전한 스캔
- scan_or_tf_stale: 스캔/TF 시각 또는 준비 문제
- release_key_or_press_k_then_command_again: k 또는 키 해제 후 재입력 필요

## 이전 수정과 지도 보존

직렬 PID210 응답 읽기가 100ms timeout 때문에 콜백을 막던 문제를 앞서 수정했다.
0 RPM 하드웨어 응답 읽기는 이전 112.18ms에서 20.15~20.40ms로 줄었다.
키보드 timeout 시 0 명령 누락도 수정했다.

지도 백업:
 /home/hprobot/amr_ws/maps/backups/before_gap_fix_20260914_1
(.pgm, .yaml, .posegraph, .data).
SLAM을 재시작하지 않는 모터 교체로 기존 지도를 보존한다.
odometry에는 피드백 부재 시 명령 기반 추정이 포함되므로 odom 증가만으로
실제 바퀴 이동이나 60cm 이동 성공을 주장해서는 안 된다.
